'use client'

import {Button} from '@/components/ui/button'

export default function CreatePostDialog(){
    return (

        <Button onClick={() => alert('Create Post coming next!')}>
            + Create Post
        </Button>
    )
}